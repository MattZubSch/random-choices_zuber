import React from "react";
import { View, Text, Button, StyleSheet, Dimensions } from 'react-native'

const PlayRandomNumber = ({ navigation }) => {
    return (
        <View>
            <Text> Play Random Number </Text>
        </View>
    )
}

export default PlayRandomNumber